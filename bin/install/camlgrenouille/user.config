# Ce fichier contient les informations pour configurer camlgrenouille
#
# Chaque ligne commençant par un '#' est un commentaire et est ignoré
# Les autres lignes sont des paires nom_de_l'option valeur séparés par
# exactement une espace
#
# OPTIONS A EDITER OBLIGATOIREMENT
#
# votre nom d'utilisateur de grenouille.com (qui est peut-être différent de
# celui des forums de grenouille.com)
username mon_nom
# 
# votre mot de passe
password mot_de_passe
#
# l'interface connectée au modem; typiquement eth0 pour linux, et en0 ou en1
# pour OS X. Pour déterminer cette interface, tapez "ifconfig" dans un terminal
# et regarder quelle interface correspond à votre adresse IP.
interface eth0
#
# Les renseignements suivants ne sont à éditer que si la configuration par
# défaut ne fonctionne pas ou si vous voulez adapter camlgrenouille à votre
# utilisation
#
# chemin de ifconfig
ifconfig /sbin/ifconfig
# 
# chemin de awk
awk /bin/awk
#
# chemin de grep
grep /bin/grep
# 
# chemin de cut
cut /usr/bin/cut
# 
# chemin de ping
ping /bin/ping
#
# delai entre l'envoi des résultats
normal_result_delay 1800
#
# mettre à "true" si les résultats doivent être envoyés
post_results true
#
# mettre à "true" si le test de ping est à effectuer
testping true
#
# mettre à "true" si le test de download est à effectuer
testdl true
#
# mettre à "true" si le test de upload est à effectuer
testul true
#
# mettre à "true" si le test de panne est à effectuer
testbk true
#
# mettre à "true" pour utiliser des connexions ftp passives
# (ceci est utile quand un pare-feu est utilisé)
passive true
#
# indiquez une adresse email si camlgrenouille ne peut pas en fabriquer une
# (utilisée pour certains tests devant se connecter en ftp anonyme)
ftp_password
#
# le plugin à utiliser: ne le changez que si vous savez exactement ce que vous
# faites
plugin "./shell_ifconfig.sh"
#
# commandes à exécuter lorsque le résultat d'un test est obtenu. Enlevez le '#'
# pour l'activer, et bien sûr mettez les à votre goût.
# test download: est appelé avec un argument, la bande passante (en ko/s). Par
# exemple:
#dl_cmd echo dl
# écrit "dl 32" si le résultat est de 32 ko/s
#
# test upload: est appelé avec un argument, la bande passante (en ko/s).
#ul_cmd echo ul
#
# test ping: est appelé avec 3 arguments: nombre de pings envoyées, nombre de
# pings reçus, temps total pris pour le test
#ping_cmd echo ping
#
# test panne: est appelé avec sans argument (seulement s'il y a une panne)
#breakdown_cmd echo BREAKDOWN
#
# Ne mettre le paramètre suivant à true que si vous avez un plugin pour votre
# routeur. Si vous ne savez pas de quoi il s'agit, laissez le à false.
detect_router false
